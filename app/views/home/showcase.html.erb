<div class="showcase-container">

  <div class="showcase-wrapper">

    <p class="showcase-title">Howdy!</p>
    <p class="showcase-description">Select what color Lockitron you want!</p>

    <div class="showcase-item-container">

      <% Array(0..7).each_with_index do |item, index| %>
          <div class="showcase-item <%= 'border-left-for-4' if index%4==0 %> <%= 'border-left-for-2' if index%2==0 %>">
            <div class="showcase-item-title">Bondi Blue</div>
            <div class="showcase-item-image">
              <%= image_tag 'theme/bondi-blue.png' %>
            </div>
            <div class="showcase-item-select">
              <%= link_to 'Select' %>
            </div>
          </div>
      <% end %>

    </div>

  </div>

</div>
